import XCTest
@testable import native_addon_swift

final class native_addon_swiftTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(native_addon_swift().text, "Hello, World!")
    }
}
