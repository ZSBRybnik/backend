enum Routes {
  Login = "/login",
  User = "/user",
  UserWithId = "/user/:id",
  Post = "/post/:id",
  VerifyToken = "/verify-token",
  GenerateRandomPassword = "/generate-random-password",
}

export default Routes;
